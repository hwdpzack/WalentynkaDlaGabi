<!DOCTYPE html>
<html lang="pl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Walentynka 💖</title>
<style>
body{
  margin:0;
  min-height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
  font-family:Arial,sans-serif;
  background:pink;
  overflow:hidden;
  position: relative;
}

/* Kontener dla serduszek w tle */
.heart-container{
  position: absolute;
  inset:0;
  pointer-events:none;
  overflow:hidden;
}

.heart{
  position: absolute;
  font-size:20px;
  color:red;
  animation-name:rise;
  animation-timing-function: linear;
  animation-iteration-count: infinite;
}

@keyframes rise {
  0% { transform: translateY(100vh) scale(1); opacity:1; }
  100% { transform: translateY(-10vh) scale(1.2); opacity:0; }
}

.box{
  position: relative;
  background: rgba(255,255,255,0.85);
  padding:30px 40px;
  border-radius:20px;
  text-align:center;
  z-index: 10;
}

h1{ color:red; }

.buttons{
  display:flex;
  gap:20px;
  justify-content:center;
  margin-top:20px;
}

button{
  font-size:18px;
  padding:10px 25px;
  border:none;
  border-radius:999px;
  cursor:pointer;
  transition: transform .2s ease;
  position: relative;
}

#yes{ background:#ff4d6d; color:#fff; }
#no{ background:#999; color:#fff; }

.hidden{ display:none; }

/* Duże serce z napisem Crack me */
.heart-box{
  margin-top:30px;
  font-size:150px;
  position: relative;
  cursor: pointer;
  display:flex;
  align-items:center;
  justify-content:center;
  color:red;
  text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
}

.heart-box span{
  position: absolute;
  font-size:40px;
  color:white;
  font-weight:bold;
}

.message{
  font-size:20px;
  color:#d00000;
  margin-top:15px;
  white-space:pre-line;
}

/* konfetti */
.confetti{
  position: fixed;
  top: -10px;
  font-size: 20px;
  animation: fall 3s linear forwards;
  pointer-events: none;
}

@keyframes fall{
  to { transform: translateY(110vh) rotate(720deg); opacity:0; }
}
</style>
</head>
<body>

<div class="heart-container" id="heartContainer"></div>

<audio id="music" loop>
<source src="https://cdn.pixabay.com/audio/2022/02/23/audio_6d7fdd6e6b.mp3" type="audio/mpeg">
</audio>

<div class="box">
  <h1>Gabi, czy chcesz być moją walentynką?</h1>
  <div class="buttons" id="buttons">
    <button id="yes">Tak</button>
    <button id="no">Nie</button>
  </div>

  <div id="heartBox" class="hidden">
    <div class="heart-box" id="heart">
      ❤️
      <span>Crack me</span>
    </div>
    <div id="message" class="message hidden">
Kocham cię ❤️
- Twój Zakus
    </div>
  </div>
</div>

<script>
const yes = document.getElementById("yes");
const no = document.getElementById("no");
const buttons = document.getElementById("buttons");
const heartBox = document.getElementById("heartBox");
const message = document.getElementById("message");
const music = document.getElementById("music");
const heart = document.getElementById("heart");
const heartContainer = document.getElementById("heartContainer");

let counter = 0, scaleYes = 1, scaleNo = 1, heartClicks = 0;

function playMusic(){ music.play().catch(()=>{}); }

function createHeart(){
  const h = document.createElement('div');
  h.className = 'heart';
  h.textContent = '❤️';
  h.style.left = Math.random()*100+'vw';
  h.style.fontSize = 10 + Math.random()*25 + 'px';
  h.style.animationDuration = 3 + Math.random()*5+'s';
  heartContainer.appendChild(h);
  setTimeout(()=> h.remove(), 8000);
}

setInterval(createHeart, 300);

function confetti(count, emoji){
  for(let i=0;i<count;i++){
    const c = document.createElement("div");
    c.className = "confetti";
    c.textContent = emoji;
    c.style.left = Math.random()*100 + "vw";
    c.style.fontSize = 16 + Math.random()*20 + "px";
    c.style.animationDuration = 2 + Math.random()*2 + "s";
    document.body.appendChild(c);
    setTimeout(()=> c.remove(), 4000);
  }
}

no.addEventListener("click",()=>{
  playMusic();
  if(counter<50){
    counter++;
    scaleYes*=1.15;
    scaleNo*=0.85;
    yes.style.transform = `scale(${scaleYes})`;
    no.style.transform = `scale(${scaleNo})`;
  }else{
    no.style.position="fixed";
    no.style.left = Math.random()*(window.innerWidth-80)+'px';
    no.style.top = Math.random()*(window.innerHeight-40)+'px';
  }
});

yes.addEventListener("click",()=>{
  playMusic();
  buttons.classList.add("hidden");
  heartBox.classList.remove("hidden");
  confetti(40,"🎉");
});

heart.addEventListener("click",()=>{
  heartClicks++;
  if(heartClicks===5){
    confetti(50,"❤️");
    message.classList.remove("hidden");
  }
});
</script>
</body>
</html>
